# Лабораторна робота №3  
## Розгортання програмного середовища та складання ПЗ для ARM (BeagleBone Black)

---

### 🎯 Мета  
Отримати практичний досвід у побудові завантажувача **U-Boot**, ядра **Linux** та мінімальної файлової системи на базі **BusyBox** для ARM-архітектури, а також запустити підготовлену систему в середовищі **QEMU**.

---

### 📁 Структура репозиторію  
- **configs/** — набір конфігурацій:  
  - *u-boot.config*  
  - *linux.config*  
  - *busybox.config*  
- **init/rcS** — базовий стартовий скрипт для `rootfs`.  
- **screenshots/** — знімки роботи зібраної системи.  
- **README.md** — документація щодо виконання лабораторної.

---

### ⚙️ Основні етапи роботи  
1. Встановлено ARM-кроскомпілятор та завантажено вихідні матеріали для **U-Boot**, **Linux kernel** і **BusyBox**.  
2. Проведено конфігурування та збирання завантажувача, ядра й статичної файлової системи на основі BusyBox.  
3. На основі підготовленого `rootfs` сформовано архів `initramfs` (`rootfs.cpio.gz`), після чого система успішно завантажена в **QEMU** з доступом до командної оболонки BusyBox.

---

### 📝 Висновок  
У межах роботи було створено та запущено мінімальну Linux-систему для ARM. Усі етапи — від встановлення інструментів до тестування в емуляторі — виконані повністю, що дозволило закріпити практичні вміння у збірці ядра, роботі з кроскомпіляцією та формуванні `initramfs`.
